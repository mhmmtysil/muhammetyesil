import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  output: 'export',
  // Eğer alt dizinde yayınlayacaksanız (ör: /muhammetyesil/), aşağıyı ekleyin:
  // basePath: '/muhammetyesil',
  // assetPrefix: '/muhammetyesil',
};

export default nextConfig;
